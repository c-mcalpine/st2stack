{
  "name": "@st2stack/ir-types",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node -e \"require('./dist/index.js'); process.exit(0)\""
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
